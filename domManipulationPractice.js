// to select the element "button" to get your button to function we go here to the javascript by creating a variable and intializing it with the value of document.
// create a variable 
// initialize it by issuing it a value of document.quereySelector


    // METHOD - Returns the first element that is a descendant of node that matches selectors.
    // what are some other options available for the METHOD? 
        //answer?: (method) ParentNode.querySelector<K extends "object" | "a" | "abbr" | "address" | "applet" | "area" | "article" | "aside" | "audio" | "b" | "base" | "basefont" | "bdi" | "bdo" | "blockquote" | "body" | "br" | "button" | "canvas" | ... 99 more ... | "wbr">(selectors: K): HTMLElementTagNameMap[K] (+2 overloads).  Returns the first element that is a descendant of node that matches selectors.
//querySelector -  The querySelector() method of the Element interface returns the first element that is a descendant of the element on which it is invoked that matches the specified group of selectors.
// SYNTAX :element = baseElement.querySelector(selectors);
     
const text = document.querySelector